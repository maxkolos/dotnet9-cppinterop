<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\StringService\StringService.csproj" />
  </ItemGroup>

  <!-- TODO: This should be unnecessary — StringService ought to build the C++ library and copy the .so
       file to dependent binaries. But with the recommended commands, the copy step doesn’t happen. -->
  <Target Name="BuildCppLibrary" BeforeTargets="Build">
    <!-- As C++ is built before the main project, make sure the output dir exists. -->
    <MakeDir Directories="$(OutputPath)" />
    <Exec Command="g++ -shared -fPIC ../CppLibrary/reverse_string.cpp -o $(OutputPath)libreverse_string.so" />
  </Target>

  <!-- Copy C++ library to the publish folder. -->
  <Target Name="CopyCppLibraryToPublish" AfterTargets="Publish">
    <Copy SourceFiles="$(OutputPath)libreverse_string.so" DestinationFolder="$(PublishDir)" />
  </Target>

</Project>
